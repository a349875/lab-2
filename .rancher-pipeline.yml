stages:
- name: repo
  steps:
  - publishImageConfig:
      dockerfilePath: ./dockerfile
      buildContext: .
      tag: a349875/k8slab01:v1
      registry: index.docker.io
- name: deploy
  steps:
  - applyYamlConfig:
      path: ./nginx-pod.yaml
timeout: 60
notification: {}
