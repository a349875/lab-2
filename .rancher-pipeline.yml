stages:
- name: repo
  steps:
  - publishImageConfig:
      dockerfilePath: ./dockerfile
      buildContext: .
      tag: a349875/k8slab01:v3
      pushRemote: true
      registry: index.docker.io
- name: deploy
  steps:
  - applyYamlConfig:
      path: ./nginx-pod-update.yaml
timeout: 60
notification: {}
